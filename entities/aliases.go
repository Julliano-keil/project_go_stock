package entities

// Aliases para compatibilidade com o código existente (domain, datastore, modules).
// Mapiam os nomes antigos para as novas structs do schema.
type (
	Category     = Categoria
	Subcategory  = SubCategoria
	StockUnit    = UnidadeEstoque
	Equipment    = Equipamento
	StockMovement = Movimentacao
)
